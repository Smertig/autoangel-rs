/** Inline style for `HoverPreview` body content (img/canvas). Caps the
 *  rendered media at the popover's body slot via `object-fit: contain`. */
export const HOVER_FIT_STYLE = { maxWidth: '100%', maxHeight: '100%', objectFit: 'contain' } as const;
